import "./Home.css";

const Home = () => {
  
  return(
  <main>
      <section id="mainContent">
        <header className="header header-home-hero">
        </header>
      </section>
    </main>
  );
};

export default Home;
